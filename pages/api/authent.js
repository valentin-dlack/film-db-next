import { ConfigService } from "../../services/config.service";

export default async function handler(req, res) {
    res.json({ status: 200, data: { message: "This page is not implemented yet" } });
}